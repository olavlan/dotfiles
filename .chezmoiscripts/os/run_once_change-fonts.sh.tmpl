{{ if eq .chezmoi.os "linux" -}}
#!/bin/bash

#set terminal font
profile=$(gsettings get org.gnome.Terminal.ProfilesList default)
profile=${profile:1:-1}
gsettings set "org.gnome.Terminal.Legacy.Profile:/org/gnome/terminal/legacy/profiles:/:$profile/" font "Monospace Bold 15"


{{ end -}}
