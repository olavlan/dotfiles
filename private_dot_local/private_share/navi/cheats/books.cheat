% books

$ html_title: find ~/main/data/books -type f -name "index.html" | while IFS= read -r file; do echo $(stat -c '%i' $file); title=$(cat $file | pup "title text{}"); echo "$title"; done | paste - -

% book, open
@ books

# open a book
python3 -m http.server <html_title>
 
