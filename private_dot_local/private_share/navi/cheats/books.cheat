% books

$ html_title: find ~/main/data/books -type f -name "index.html" | sort | while IFS= read -r file; do title=$(cat $file | pup "title text{}"); echo "$title"; done | nl

% book, open
@ books

# open a book
python3 -m http.server <html_title>
 
