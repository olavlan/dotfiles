% books

$ html_filename: find ~/main/data/books -type f -name "index.html" 
$ html_title: echo "<html_filename>" | xargs -I{} sh -c 'pup "title text{}" < "{}"'
% book, open
@ books

# open a book
python3 -m http.server <html_title>
 
