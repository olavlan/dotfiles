% books

$ html_title: find ~/main/data/books -type f -name "index.html" | while IFS= read -r file; do title=$(cat $file | pup "title text{}"); echo $title; done

% book, open
@ books

# open a book
python3 -m http.server <html_title>
 
