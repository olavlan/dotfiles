% chezmoi 

$ config_file: chezmoi managed

% config
@ chezmoi

# change a config file
cd
chezmoi edit -a <config_file>

% config, backup
@ chezmoi

# backup config files
chezmoi cd
git add .
git commit -m "update"
git push origin main
